from setuptools import setup, find_packages

setup(name='gym_2d_perception',
      version='0.0.1',
      packages=find_packages(),
      install_requires=['gym']#And any other dependencies required
)